
pub mod jtoken;
pub mod jpath_token;
pub mod utility;
pub mod jpath_token_type;
mod json_error;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
