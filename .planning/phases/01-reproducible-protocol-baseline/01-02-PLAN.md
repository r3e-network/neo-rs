---
phase: 01-reproducible-protocol-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Cargo.toml
  - Cargo.lock
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - deny.toml
  - benches-package/Cargo.toml
  - neo-rpc/Cargo.toml
  - neo-serialization/Cargo.toml
  - neo-serialization/src/codec/serialization.rs
  - neo-serialization/src/lib.rs
  - neo-serialization/src/tests/codec/serialization.rs
  - neo-test-fixtures/Cargo.toml
  - tests/Cargo.toml
  - Dockerfile
  - docker-compose.yml
  - .github/workflows/ci.yml
  - .github/workflows/compatibility-v310.yml
  - .github/workflows/fuzz.yml
  - scripts/validate-v310-consistency.sh
  - scripts/tests/test_dependency_hygiene.py
  - scripts/tests/test_protocol_target_docs.py
  - docs/architecture.md
  - docs/protocol-compatibility.md
  - design.md
  - .planning/phases/01-reproducible-protocol-baseline/01-REPRODUCIBILITY-EVIDENCE.md
autonomous: true
requirements:
  - PROTO-01
  - BUILD-01
must_haves:
  truths:
    - "Root, fuzz, CI, and Docker resolve the same immutable neo-vm-rs revision without a sibling checkout."
    - "Every Cargo resolution path is locked, all installed Rust tools are versioned, and retry exhaustion remains a nonzero failure."
    - "Root and standalone fuzz dependency graphs pass the same advisory, license, and source policy."
    - "Maintained architecture and protocol documentation describes the official v3.10.1 schedules and the sole canonical local engine without overstating parity evidence."
    - "One clean detached source commit passes the full locked suite and produces a runnable no-cache Docker image with retained evidence."
  artifacts:
    - path: "scripts/tests/test_dependency_hygiene.py"
      provides: "Parsed root/fuzz dependency, pin, lock, and bincode policy guards"
    - path: "scripts/tests/test_protocol_target_docs.py"
      provides: "Workflow failure-semantics and maintained-document guards"
    - path: ".github/workflows/ci.yml"
      provides: "Rust 1.89 locked workspace and dual cargo-deny gates"
    - path: ".github/workflows/fuzz.yml"
      provides: "Dated-nightly fuzzing with a pinned cargo-fuzz and immutable standalone lock"
    - path: "design.md"
      provides: "ADR-044 for the immutable VM and canonical execution boundary"
      contains: "ADR-044"
    - path: ".planning/phases/01-reproducible-protocol-baseline/01-REPRODUCIBILITY-EVIDENCE.md"
      provides: "Commit-anchored source, dependency, workflow, and container proof"
  key_links:
    - from: "Cargo.toml"
      to: "fuzz/Cargo.toml"
      via: "identical neo-vm-rs v0.2.0 Git URL, full revision, features, and disabled defaults"
      pattern: "3081e83db3716fd51dc58c0afc039290d2d07253"
    - from: ".github/workflows/fuzz.yml"
      to: "fuzz/Cargo.lock"
      via: "locked metadata plus before/after checksum around every fuzz command"
      pattern: "nightly-2025-11-30|0.13.1|Cargo.lock"
    - from: ".github/workflows/compatibility-v310.yml"
      to: "scripts/validate-v310-consistency.sh"
      via: "failure-preserving retry and explicitly fallible RPC endpoint selection"
      pattern: "rc=\\$\\?|select_rpc"
    - from: "docs/protocol-compatibility.md"
      to: "neo-config/src/settings/hardfork.rs"
      via: "official Gorgon activation heights and unscheduled Huyao"
      pattern: "12,020,000|17,960,000|HF_Huyao"
    - from: "Dockerfile"
      to: ".planning/phases/01-reproducible-protocol-baseline/01-REPRODUCIBILITY-EVIDENCE.md"
      via: "Rust 1.89 locked build from a detached repository-only context and neo-node version smoke test"
      pattern: "cargo build --release --locked|neo-node --version"
---

<objective>
Turn the semantic baseline from Plan 01-01 into a reproducible, fail-closed build proof and accurate architecture record.

Purpose: BUILD-01 is not satisfied by source inspection or cached results; the exact locked graph, workflows, scripts, documentation, and clean container must agree on one committed source tree.
Output: Locked/pinned CI and fuzz workflows, structured repository guards, corrected protocol/architecture docs, ADR-044, and retained clean-build/container evidence.
</objective>

<execution_context>
@/home/neo/.codex/gsd-core/workflows/execute-plan.md
@/home/neo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reproducible-protocol-baseline/01-CONTEXT.md
@.planning/phases/01-reproducible-protocol-baseline/01-RESEARCH.md
@.planning/phases/01-reproducible-protocol-baseline/01-VALIDATION.md
@.planning/phases/01-reproducible-protocol-baseline/01-PATTERNS.md
@.planning/phases/01-reproducible-protocol-baseline/01-01-SUMMARY.md

Plan 01-01 is a hard dependency because this plan records and builds the exact
semantic source tree it validated. The current dependency locks, bincode helper
removal, cargo-deny policy, Dockerfile, compose file, and crate-license metadata
are already partially remediated in the worktree. Inspect and preserve those
edits; finish only the identified reproducibility gaps.
</context>

## Artifacts This Phase Produces

- Locked Rust 1.89 CI and compatibility jobs with `cargo-nextest` 0.9.128 and `cargo-deny` 0.18.9.
- Dated `nightly-2025-11-30` fuzz jobs using `cargo-fuzz` 0.13.1 and an immutable `fuzz/Cargo.lock`.
- Parsed dependency guards and workflow/documentation regression guards under `scripts/tests/`.
- Updated `docs/architecture.md`, `docs/protocol-compatibility.md`, and accepted ADR-044 in `design.md`.
- `.planning/phases/01-reproducible-protocol-baseline/01-REPRODUCIBILITY-EVIDENCE.md` and `01-02-SUMMARY.md`.

<tasks>

<task type="auto">
  <name>Task 1: Make dependency, CI, compatibility, and fuzz resolution fail closed</name>
  <files>Cargo.toml, Cargo.lock, fuzz/Cargo.toml, fuzz/Cargo.lock, deny.toml, benches-package/Cargo.toml, neo-rpc/Cargo.toml, neo-serialization/Cargo.toml, neo-serialization/src/codec/serialization.rs, neo-serialization/src/lib.rs, neo-serialization/src/tests/codec/serialization.rs, neo-test-fixtures/Cargo.toml, tests/Cargo.toml, Dockerfile, docker-compose.yml, .github/workflows/ci.yml, .github/workflows/compatibility-v310.yml, .github/workflows/fuzz.yml, scripts/validate-v310-consistency.sh, scripts/tests/test_dependency_hygiene.py, scripts/tests/test_protocol_target_docs.py</files>
  <read_first>
    - .planning/phases/01-reproducible-protocol-baseline/01-RESEARCH.md
    - .planning/phases/01-reproducible-protocol-baseline/01-PATTERNS.md
    - Cargo.toml
    - fuzz/Cargo.toml
    - deny.toml
    - .github/workflows/ci.yml
    - .github/workflows/compatibility-v310.yml
    - .github/workflows/fuzz.yml
    - scripts/validate-v310-consistency.sh
    - scripts/tests/test_dependency_hygiene.py
    - scripts/tests/test_protocol_target_docs.py
  </read_first>
  <action>
Reconcile the existing dependency remediation first. Root and fuzz manifests must declare the identical `neo-vm-rs` v0.2.0 Git dependency at full revision `3081e83db3716fd51dc58c0afc039290d2d07253`, with `default-features = false` and `std,interpreter`; both locks must resolve that exact source. Preserve the fixed `anyhow 1.0.103`, `crossbeam-epoch 0.9.20`, and fuzz `num-bigint 0.4.8` resolutions. Keep generic bincode helpers removed from `neo-serialization`; bincode may remain only for `neo-consensus` recovery bytes under the documented `RUSTSEC-2025-0141` exception tracked to Phase 2 plan 02-03. Do not change those persisted bytes silently. Keep `deny.toml` deny-by-default for unknown Git sources and allow only the pinned VM repository, with reviewed BSL-1.0 and NCSA license entries.

Harden `.github/workflows/ci.yml` in place: use Rust `1.89.0` in every Rust job, add `--locked` to metadata, Clippy, test compilation, nextest, and doctest resolution, retain `-D warnings`, install `cargo-nextest@0.9.128` through an explicit `taiki-e/install-action` release reference and tool input, and add a job that installs `cargo-deny@0.18.9` and checks advisories, licenses, and sources for both root and `fuzz/Cargo.toml`. Do not create a second CI framework.

In `.github/workflows/compatibility-v310.yml`, add `neo-primitives/**` to both path-trigger lists, pin Rust `1.89.0`, and capture the validator's nonzero status immediately inside the `else` branch before retry logging. Exhausting all attempts must exit with the last nonzero status. In `scripts/validate-v310-consistency.sh`, catch failed `select_rpc` substitutions at the assignment so `set -e` does not exit before the explicit reference-unavailable handling. An unavailable reference is supplemental infrastructure status and must not be labeled parity success.

In every fuzz job, pin `nightly-2025-11-30` and `cargo-fuzz` 0.13.1, run locked standalone metadata before fuzzing, hash `fuzz/Cargo.lock` before each fuzz command, and fail if the checksum changes afterward. Extend `test_dependency_hygiene.py` with `tomllib` parsing for root/fuzz manifest and lock invariants, Rust version, Git revision/source, bincode scope, and cargo-deny policy. Extend `test_protocol_target_docs.py` with targeted workflow-block checks for locked Cargo commands, exact tool versions, both compatibility triggers, retry status placement, fallible RPC assignments, and lock-integrity steps. Use actionlint for YAML/shell syntax and precise block/regex assertions for semantics; do not use a broad unstructured count as the dependency authority.
  </action>
  <verify>
    <automated>actionlint -no-color &amp;&amp; bash -n scripts/validate-v310-consistency.sh &amp;&amp; python3 -m unittest scripts.tests.test_dependency_hygiene scripts.tests.test_protocol_target_docs &amp;&amp; cargo deny check advisories licenses sources --hide-inclusion-graph &amp;&amp; cargo deny --manifest-path fuzz/Cargo.toml check advisories licenses sources --hide-inclusion-graph &amp;&amp; cargo +1.89.0 metadata --manifest-path fuzz/Cargo.toml --locked --no-deps --format-version 1 &gt;/dev/null &amp;&amp; cargo +1.89.0 check --manifest-path fuzz/Cargo.toml --locked --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - Structured tests prove root/fuzz pins and lock sources are identical and immutable.
    - Every resolving Cargo command in the three Phase 1 workflows is locked; formatting-only commands remain exempt because they do not resolve dependencies.
    - Stable jobs use Rust 1.89.0; nextest is 0.9.128; cargo-deny is 0.18.9; fuzz uses nightly-2025-11-30 and cargo-fuzz 0.13.1.
    - Three failed compatibility attempts produce a nonzero final status, while failed endpoint selection reaches the script's explicit availability policy.
    - Fuzz jobs detect any mutation of `fuzz/Cargo.lock`.
    - Root and fuzz cargo-deny advisory/license/source checks pass; fuzz contains no bincode and root bincode remains scoped to consensus recovery.
  </acceptance_criteria>
  <done>All dependency and workflow resolution surfaces consume reviewed immutable inputs and preserve failures instead of masking them.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the active protocol and architecture contract in ADR-044</name>
  <files>docs/architecture.md, docs/protocol-compatibility.md, design.md, scripts/tests/test_protocol_target_docs.py</files>
  <read_first>
    - .planning/phases/01-reproducible-protocol-baseline/01-CONTEXT.md
    - .planning/phases/01-reproducible-protocol-baseline/01-RESEARCH.md
    - docs/architecture.md
    - docs/protocol-compatibility.md
    - design.md
    - scripts/tests/test_protocol_target_docs.py
  </read_first>
  <action>
Correct `docs/protocol-compatibility.md` to record the official `neo-project/neo-node` v3.10.1 configuration authority at tag commit `7313f8087724e1de4caa88edd2ada58c1fe54abc`, Gorgon at MainNet 12,020,000 and TestNet 17,960,000, and Huyao as unscheduled in both built-in presets. Preserve the distinction between an empty loaded hardfork object, which backfills known forks at height zero for C# compatibility, and explicit operational schedules.

Update `docs/architecture.md` so the two-tier VM diagram and prose describe `neo-vm-rs` as an immutable Git dependency rather than a filesystem input. State that `neo-vm` owns stateful object identity and hardfork-aware canonical execution, while the lean interpreter remains non-canonical until differential proof. Update every active ADR count to 44 without changing historical decision text that is still accurate.

Update `design.md` metadata/count summaries and append ADR-044 using the established ADR-043 shape. Record the exact VM revision; official Neo v3.10.1 source/configuration as protocol authority; reth/Substrate as architecture references only; the local hardfork-aware engine as the sole canonical route; compound-ID alias/conflict behavior; pre-/post-Domovoi notification identity/immutability behavior; and the temporary bincode recovery exception with its Phase 2 migration tracker. Explicitly state that Phase 1 evidence does not establish full differential, live-network, or MainNet replay parity. Correct stale sibling wording in current architecture prose without rewriting the substance of older ADRs. Extend the maintained-doc guard with exact schedule, immutable dependency, ADR-044, canonical-engine, and evidence-scope markers.
  </action>
  <verify>
    <automated>python3 -m unittest scripts.tests.test_protocol_target_docs scripts.tests.test_architecture_docs &amp;&amp; rg -q '12,020,000' docs/protocol-compatibility.md &amp;&amp; rg -q '17,960,000' docs/protocol-compatibility.md &amp;&amp; rg -q 'ADR-044' design.md</automated>
  </verify>
  <acceptance_criteria>
    - The maintained schedule table matches the official v3.10.1 MainNet/TestNet Gorgon boundaries and leaves Huyao unscheduled.
    - Architecture prose names the immutable Git revision boundary and identifies the local VM as canonical.
    - ADR counts are 44 wherever the active documentation summarizes the log.
    - ADR-044 records authority, engine, identity/immutability, dependency, trade-off, reversibility, and evidence-scope decisions.
    - Documentation tests prevent the stale schedule, filesystem dependency model, or unsupported parity claims from returning.
  </acceptance_criteria>
  <done>Maintained docs and ADR-044 accurately describe the reproducible v3.10.1 protocol baseline and its evidence limits.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the final committed source and clean container</name>
  <files>.planning/phases/01-reproducible-protocol-baseline/01-REPRODUCIBILITY-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/01-reproducible-protocol-baseline/01-VALIDATION.md
    - .planning/phases/01-reproducible-protocol-baseline/01-01-SUMMARY.md
    - Cargo.toml
    - fuzz/Cargo.toml
    - deny.toml
    - Dockerfile
    - docker-compose.yml
  </read_first>
  <action>
Do not reuse command output or a Docker image produced before Tasks 1 and 2. After the semantic, workflow, dependency, and documentation edits are committed, require an empty `git status --short` and capture the full commit SHA. From that source run the complete Rust 1.89.0 locked gate: workspace format check, workspace test-aware check, full workspace tests, explicit doctests, and all-target Clippy with warnings denied. Run stable locked format/metadata/all-target checks for the standalone fuzz package, both root/fuzz cargo-deny graphs, actionlint, Bash syntax over every tracked shell script, and the complete Python repository-test discovery suite. A failed command blocks evidence creation.

Create a temporary detached worktree at the captured commit. Record that its status is clean, then run `docker build --pull --no-cache` from that worktree's repository root with a commit-specific image tag. Run the exact image with `--entrypoint neo-node --version`, inspect its immutable image ID, and remove the temporary worktree after recording results. The Docker context must contain no sibling repository or additional context.

Create `01-REPRODUCIBILITY-EVIDENCE.md` containing the full commit SHA, UTC timestamp, Rust/Cargo/cargo-deny/actionlint/Docker versions, exact commands, per-command exit status, root and fuzz VM source/revision, lock checksums, Docker image tag and ID, and the complete `neo-node --version` smoke output. If optional live reference validation is attempted, record unreachable runs as unavailable and exclude them from the success verdict. Do not claim differential parity, sustained peer interoperability, full MainNet replay, or authenticated fast sync in this artifact.
  </action>
  <verify>
    <automated>cargo +1.89.0 fmt --all -- --check &amp;&amp; cargo +1.89.0 check --workspace --tests --locked &amp;&amp; cargo +1.89.0 test --workspace --locked &amp;&amp; cargo +1.89.0 test --workspace --doc --locked &amp;&amp; cargo +1.89.0 clippy --workspace --all-targets --locked -- -D warnings &amp;&amp; cargo +1.89.0 fmt --manifest-path fuzz/Cargo.toml -- --check &amp;&amp; cargo +1.89.0 metadata --manifest-path fuzz/Cargo.toml --locked --no-deps --format-version 1 &gt;/dev/null &amp;&amp; cargo +1.89.0 check --manifest-path fuzz/Cargo.toml --locked --all-targets &amp;&amp; cargo deny check advisories licenses sources --hide-inclusion-graph &amp;&amp; cargo deny --manifest-path fuzz/Cargo.toml check advisories licenses sources --hide-inclusion-graph &amp;&amp; actionlint -no-color &amp;&amp; find scripts -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n &amp;&amp; python3 -m unittest discover -s scripts/tests -p 'test_*.py' &amp;&amp; test -s .planning/phases/01-reproducible-protocol-baseline/01-REPRODUCIBILITY-EVIDENCE.md &amp;&amp; rg -q 'neo-node --version' .planning/phases/01-reproducible-protocol-baseline/01-REPRODUCIBILITY-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - All workspace, fuzz, supply-chain, workflow, shell, and Python gates pass from one final committed source tree.
    - A detached clean worktree at that exact commit builds with `--pull --no-cache` and no sibling input.
    - The built image runs `neo-node --version` successfully and its immutable image ID is retained.
    - The evidence artifact records exact commands, tool versions, exit statuses, lock identities, commit SHA, and smoke output.
    - Supplemental endpoint unavailability is never presented as protocol parity.
  </acceptance_criteria>
  <done>The final committed Phase 1 source has reproducible locked source, fuzz, policy, and container proof retained in a reviewable artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `manifests/locks -> CI and fuzz runners` | Repository policy selects remote registry/Git code and executable build tools. |
| `workflow/script exit status -> retained evidence` | Shell control flow decides whether a failed compatibility or build run is reported as successful. |
| `Git commit -> Docker build context -> image` | The claimed source tree crosses into a remote-fetching container build and runnable release artifact. |
| `official Neo sources -> maintained docs/ADR` | External protocol facts become the local engineering contract used by later phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | Root/fuzz dependency graphs and tool installers | high | mitigate | Exact VM/tool revisions, Cargo `--locked`, standalone lock checksums, dual cargo-deny advisory/license/source gates, and parsed repository guards. |
| T-01-04 | Repudiation / Denial of Service | Compatibility retry, RPC selection, and maintained evidence docs | high | mitigate | Capture nonzero status inside `else`, catch expected probe failures explicitly, test the control-flow markers, and exclude unavailable references from parity evidence. |
| T-01-05 | Spoofing / Tampering | Detached Docker build and evidence artifact | high | mitigate | Anchor to one clean commit, build with no cache or sibling context, retain image ID and smoke output, and record every command/status. |
| T-01-SC | Tampering | Existing Cargo tool/package installation surfaces | high | mitigate | Use the research-reviewed existing tools only, pin nextest 0.9.128, cargo-deny 0.18.9, nightly-2025-11-30, and cargo-fuzz 0.13.1, and block on dependency/source policy. |

No critical or high threat is accepted. All high threats are release-blocking for BUILD-01.
</threat_model>

<verification>
1. Run the Task 1 workflow, dependency, standalone fuzz, and dual cargo-deny command.
2. Run the Task 2 documentation and architecture guards.
3. Run every locked workspace/fuzz source gate in Task 3 from the final committed source.
4. Build a commit-tagged image with `docker build --pull --no-cache` from a clean detached worktree.
5. Run `neo-node --version`, inspect the image ID, and confirm the evidence artifact records the same commit and image.
</verification>

<success_criteria>
- PROTO-01 is reproducible across root/fuzz manifests, locks, workflows, Docker, and architecture documentation.
- BUILD-01 passes clean locked workspace, fuzz, dependency-policy, repository-test, and container gates without undeclared local input.
- Every workflow failure path remains a failure unless it is explicitly classified as unavailable supplemental evidence.
- ADR-044 and active docs state what Phase 1 proves and do not borrow protocol authority from reth or Substrate.
</success_criteria>

<output>
Create `.planning/phases/01-reproducible-protocol-baseline/01-02-SUMMARY.md` when done.
</output>
