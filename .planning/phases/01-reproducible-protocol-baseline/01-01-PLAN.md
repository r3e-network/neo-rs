---
phase: 01-reproducible-protocol-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - neo-vm/src/stack_item/stack_item.rs
  - neo-vm/src/stack_item/array.rs
  - neo-vm/src/stack_item/buffer.rs
  - neo-vm/src/stack_item/map.rs
  - neo-vm/src/stack_item/struct_item.rs
  - neo-vm/src/tests/stack_item/stack_item.rs
  - neo-payloads/src/execution/notify_event_args.rs
  - neo-payloads/src/tests/execution/notify_event_args.rs
  - neo-execution/src/interop/application_engine_helper.rs
  - neo-execution/src/application_engine/storage_ops/load_execute_storage.rs
  - neo-execution/src/tests/interop/application_engine_runtime.rs
  - neo-config/src/settings/hardfork.rs
  - neo-config/src/tests/settings/hardfork.rs
  - neo-config/src/tests/settings/protocol.rs
  - neo-blockchain/src/state_root/consensus.rs
  - neo-blockchain/src/state_root/tests/consensus.rs
autonomous: true
requirements:
  - PROTO-01
  - CONSENSUS-01
must_haves:
  truths:
    - "Repeated neo-vm-rs compound IDs reconstruct as one local object, while conflicting definitions fail closed."
    - "Notification state follows Neo v3.10.1 identity and immutability rules on both sides of Domovoi."
    - "MainNet and TestNet activate every scheduled hardfork at the official Neo v3.10.1 boundary and leave Huyao unscheduled."
    - "Canonical execution uses the local hardfork-aware VM and cannot automatically dispatch to the external interpreter."
    - "State-root votes cannot share a quorum across version, block index, or root hash."
  artifacts:
    - path: "neo-vm/src/stack_item/stack_item.rs"
      provides: "Identity-aware StackValue to StackItem graph reconstruction"
      contains: "StackValueConversion"
    - path: "neo-execution/src/interop/application_engine_helper.rs"
      provides: "Pre-/post-Domovoi notification projection policy"
      contains: "get_notifications"
    - path: "neo-config/src/tests/settings/hardfork.rs"
      provides: "Complete official MainNet and TestNet activation-boundary regression tables"
    - path: "neo-blockchain/src/state_root/consensus.rs"
      provides: "Full state-root vote aggregation identity"
      contains: "StateRootIdentity"
  key_links:
    - from: "neo-vm/src/stack_item/stack_item.rs"
      to: "neo-vm/src/tests/stack_item/stack_item.rs"
      via: "TryFrom<StackValue> alias preservation and conflict rejection"
      pattern: "StackValueConversion|preserves_repeated_compound_aliases"
    - from: "neo-payloads/src/execution/notify_event_args.rs"
      to: "neo-execution/src/interop/application_engine_helper.rs"
      via: "stored state_array before Domovoi and immutable memoized copy after Domovoi"
      pattern: "state_array|HfDomovoi"
    - from: "neo-config/src/settings/hardfork.rs"
      to: "neo-execution/src/application_engine/storage_ops/load_execute_storage.rs"
      via: "configured hardfork rules consumed by the sole canonical local engine"
      pattern: "is_hardfork_enabled|execute_allow_fault"
    - from: "neo-blockchain/src/state_root/consensus.rs"
      to: "neo-blockchain/src/state_root/tests/consensus.rs"
      via: "vote pool keyed by version, index, and root hash"
      pattern: "StateRootIdentity|distinct_block_indexes"
---

<objective>
Finish and validate the already-partial NeoVM v0.2 semantic migration without replacing the dirty-worktree implementation.

Purpose: Establish the consensus-safe half of the reproducible Neo v3.10.1 baseline before build and container evidence is generated.
Output: Alias-preserving VM bridges, exact notification and hardfork behavior, a canonical local execution path, full state-root vote isolation, and a locked Rust 1.89 workspace check.
</objective>

<execution_context>
@/home/neo/.codex/gsd-core/workflows/execute-plan.md
@/home/neo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reproducible-protocol-baseline/01-CONTEXT.md
@.planning/phases/01-reproducible-protocol-baseline/01-RESEARCH.md
@.planning/phases/01-reproducible-protocol-baseline/01-VALIDATION.md
@.planning/phases/01-reproducible-protocol-baseline/01-PATTERNS.md

The worktree already contains the broad neo-vm-rs v0.2 API migration across VM,
manifest, payload, native-contract, node, RPC, serialization, and fuzz callers.
Treat that diff as implementation to reconcile and verify. Do not reset it,
regenerate it wholesale, or use the compiler gate as permission for unrelated
crate refactors.

<interfaces>
The existing contracts that this plan preserves are:

- `impl TryFrom<neo_vm_rs::StackValue> for StackItem`
- `StackValueConversion::convert(&mut self, StackValue) -> VmResult<StackItem>`
- `NotifyEventArgs::state_array(&self) -> StackItem`
- `ApplicationEngine::get_notifications(&self, Option<UInt160>) -> CoreResult<Vec<StackItem>>`
- `ApplicationEngine::execute_allow_fault(&mut self) -> VMState`
- `StateRootCollector::add_vote(...) -> Option<Witness>`
- `HardforkManager::{mainnet,testnet,is_enabled}`
</interfaces>
</context>

## Artifacts This Phase Produces

- A compiler-complete migration to `neo-vm-rs` v0.2.0 at revision `3081e83db3716fd51dc58c0afc039290d2d07253`.
- Behavioral regressions for compound aliases, conflicting compound definitions, notification identity/immutability, hardfork boundaries, canonical engine selection, and state-root vote identity.
- `.planning/phases/01-reproducible-protocol-baseline/01-01-SUMMARY.md` with the exact focused and workspace checks run.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile compound graph and notification semantics</name>
  <files>neo-vm/src/stack_item/stack_item.rs, neo-vm/src/stack_item/array.rs, neo-vm/src/stack_item/buffer.rs, neo-vm/src/stack_item/map.rs, neo-vm/src/stack_item/struct_item.rs, neo-vm/src/tests/stack_item/stack_item.rs, neo-payloads/src/execution/notify_event_args.rs, neo-payloads/src/tests/execution/notify_event_args.rs, neo-execution/src/interop/application_engine_helper.rs, neo-execution/src/tests/interop/application_engine_runtime.rs</files>
  <read_first>
    - .planning/phases/01-reproducible-protocol-baseline/01-CONTEXT.md
    - .planning/phases/01-reproducible-protocol-baseline/01-PATTERNS.md
    - neo-vm/src/stack_item/stack_item.rs
    - neo-vm/src/tests/stack_item/stack_item.rs
    - neo-payloads/src/execution/notify_event_args.rs
    - neo-execution/src/interop/application_engine_helper.rs
    - neo-execution/src/tests/interop/application_engine_runtime.rs
  </read_first>
  <behavior>
    - Repeating one Buffer, Array, Struct, or Map ID yields one shared local allocation; mutation through one mutable occurrence is observed through the other.
    - Reusing one compound ID with different content or a different compound kind returns a typed VM error before a conflicting graph is exposed.
    - Before Domovoi, repeated `GetNotifications` calls reuse the notification's stored immutable state array and nested aliases.
    - At Domovoi and later, each `GetNotifications` call returns a fresh immutable deep copy while preserving aliases inside that copy.
    - Read-only mutation attempts fail in both hardfork branches.
  </behavior>
  <action>
Preserve and audit the existing memoized `StackValueConversion` implementation instead of rebuilding it. Confirm that Buffer, Array, Struct, and Map register an empty or complete local object before recursive descent, reuse the same `Arc` for an already-seen ID, and compare the repeated definition before returning it. Keep compound-ID width conversion fallible and return the crate-standard `VmError`; do not introduce a panic or silently accept a conflicting kind/shape.

Preserve `NotifyEventArgs` ownership of one immutable state-array object. Keep hardfork policy in `neo-execution`: pre-Domovoi returns that stored object, while Domovoi and later use one memo table to create a fresh immutable graph, convert Buffer to ByteString as Neo does, and preserve repeated internal references. Treat the existing behavioral tests as the contract; if a correction is needed, first add or tighten the failing case, observe it fail, then make the smallest implementation correction. Official Neo `d10e9ceecdabe3fcff719ee68ea5b76ba7e62c3d` and Neo.VM `004cd6070a940405818d9357638277dd44407e2e` are the semantic authorities.
  </action>
  <verify>
    <automated>cargo +1.89.0 test --locked -p neo-vm stack_value &amp;&amp; cargo +1.89.0 test --locked -p neo-payloads notify_event_args &amp;&amp; cargo +1.89.0 test --locked -p neo-execution get_notifications</automated>
  </verify>
  <acceptance_criteria>
    - All four compound kinds have observable shared-allocation regressions.
    - Conflicting same-ID content and same-ID kind are rejected by tests.
    - Notification tests assert object identity, nested alias identity, immutability, and repeated-call behavior on both sides of Domovoi.
    - Conversion errors remain typed at crate boundaries and no new panic path is added.
  </acceptance_criteria>
  <done>The existing graph bridge and notification implementation satisfy the official v3.10.1 identity and immutability behavior under focused locked tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock hardfork execution and state-root quorum identities</name>
  <files>neo-config/src/settings/hardfork.rs, neo-config/src/tests/settings/hardfork.rs, neo-config/src/tests/settings/protocol.rs, neo-execution/src/application_engine/storage_ops/load_execute_storage.rs, neo-execution/src/tests/interop/application_engine_runtime.rs, neo-blockchain/src/state_root/consensus.rs, neo-blockchain/src/state_root/tests/consensus.rs</files>
  <read_first>
    - .planning/phases/01-reproducible-protocol-baseline/01-RESEARCH.md
    - .planning/phases/01-reproducible-protocol-baseline/01-PATTERNS.md
    - neo-config/src/settings/hardfork.rs
    - neo-config/src/tests/settings/hardfork.rs
    - neo-execution/src/application_engine/storage_ops/load_execute_storage.rs
    - neo-execution/src/tests/interop/application_engine_runtime.rs
    - neo-blockchain/src/state_root/consensus.rs
    - neo-blockchain/src/state_root/tests/consensus.rs
  </read_first>
  <behavior>
    - Every scheduled MainNet and TestNet hardfork is disabled at `height - 1` and enabled at its official activation height.
    - MainNet Gorgon activates at 12,020,000, TestNet Gorgon activates at 17,960,000, and Huyao remains absent from both built-in schedules.
    - Canonical `execute_allow_fault` invokes only the local hardfork-aware VM; the zero-shift divergence sentinel continues to prove why external dispatch is not allowed.
    - A valid competing vote with a different root hash, version, or block index cannot complete another identity's quorum.
    - Matching valid votes still produce the expected multisig witness.
  </behavior>
  <action>
Audit the existing table-driven MainNet/TestNet fixtures against the official `neo-project/neo-node` v3.10.1 configuration recorded in research. Require complete schedule length, exact heights, both sides of every boundary, and the unscheduled Huyao case. Keep the empty-hardfork-object compatibility rule separate from the built-in operational schedules.

Keep `ApplicationEngine::execute_allow_fault` attached to the local stateful engine and retain both the source guard and zero-shift behavioral sentinel. The external interpreter may remain as a non-canonical differential component, but no automatic selection, fallback, environment switch, or feature path may route block execution through it before Phase 3 equivalence evidence exists.

Keep the vote map keyed by typed `(version, index, root_hash)` identity and validate signatures before mutating the pool. Retain one adversarial test for each identity field plus the positive aggregation control. Then install the Rust 1.89.0 toolchain with rustfmt/clippy if it is absent, run `cargo +1.89.0 fmt --all`, and use `cargo +1.89.0 check --workspace --tests --locked` as the compiler-driven audit of the already-present v0.2 call-site migration. Correct only concrete compiler/test failures caused by that migration, following the owning crate's existing typed-error and StackValue projection pattern, and record every additional corrected path in the summary.
  </action>
  <verify>
    <automated>cargo +1.89.0 test --locked -p neo-config hardfork &amp;&amp; cargo +1.89.0 test --locked -p neo-execution canonical_execution &amp;&amp; cargo +1.89.0 test --locked -p neo-execution zero_shift &amp;&amp; cargo +1.89.0 test --locked -p neo-blockchain state_root::consensus::tests &amp;&amp; cargo +1.89.0 check --workspace --tests --locked &amp;&amp; cargo +1.89.0 fmt --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - The hardfork fixtures cover all seven scheduled forks for each network and the Huyao absence rule.
    - The canonical execution guard and behavioral divergence sentinel both pass.
    - Root-hash, version, and block-index vote isolation tests pass independently, along with a matching-vote positive control.
    - The entire workspace and its tests compile with Rust 1.89.0 using the committed lockfile.
    - Formatting is stable and any migration correction is named in `01-01-SUMMARY.md`.
  </acceptance_criteria>
  <done>Official hardfork selection, canonical local execution, state-root quorum identity, and the broad v0.2 migration all pass their locked Rust 1.89 compiler and behavioral gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `neo-vm-rs StackValue -> local StackItem` | An external tree/ID representation crosses into mutable, reference-counted consensus execution state. |
| `configured block height -> execution semantics` | Network configuration selects consensus behavior at historical hardfork boundaries. |
| `signed StateRoot -> vote pool` | Peer-originated signed payloads enter quorum aggregation state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Denial of Service | StackValue bridge and notification projection | high | mitigate | Memoize every compound ID, reject conflicting definitions with typed errors, preserve official immutable-copy rules, and block on focused alias/conflict tests. |
| T-01-02 | Spoofing / Tampering | Hardfork selection, canonical engine, and StateRootCollector | high | mitigate | Pin official activation tables, forbid automatic external-engine dispatch, validate signatures before mutation, key pools by the full identity tuple, and retain adversarial tests for every field. |

No critical or high threat is accepted. Both high threats are release-blocking for this plan.
</threat_model>

<verification>
1. `cargo +1.89.0 test --locked -p neo-vm stack_value`
2. `cargo +1.89.0 test --locked -p neo-payloads notify_event_args`
3. `cargo +1.89.0 test --locked -p neo-execution get_notifications`
4. `cargo +1.89.0 test --locked -p neo-config hardfork`
5. `cargo +1.89.0 test --locked -p neo-execution canonical_execution`
6. `cargo +1.89.0 test --locked -p neo-execution zero_shift`
7. `cargo +1.89.0 test --locked -p neo-blockchain state_root::consensus::tests`
8. `cargo +1.89.0 check --workspace --tests --locked`
9. `cargo +1.89.0 fmt --all -- --check`
</verification>

<success_criteria>
- PROTO-01's local hardfork-aware execution semantics are enforced by behavior, not only source inspection.
- CONSENSUS-01 has independent adversarial coverage for version, block index, and root hash.
- The existing 116-file migration is preserved and compiler-reconciled rather than destructively regenerated.
- No Phase 1 result is represented as full differential, live-network, or MainNet replay parity; those remain in their assigned later phases.
</success_criteria>

<output>
Create `.planning/phases/01-reproducible-protocol-baseline/01-01-SUMMARY.md` when done.
</output>
