# Neo Rust Node (docker-compose) environment defaults
NEO_NETWORK=testnet
# Uncomment to point at a custom config inside the container
# NEO_CONFIG=/config/neo_custom.toml
# Leave empty to auto-select /data/testnet or /data/mainnet based on NEO_NETWORK
# NEO_STORAGE=/data/blockchain
# Set to rocksdb to persist the chain (default); leave empty to use TOML default
NEO_BACKEND=rocksdb
NEO_PLUGINS_DIR=/data/Plugins
# Override RPC or P2P ports if you change them in the config
# NEO_RPC_PORT=20332 # optional: entrypoint will try to read [rpc].port from the TOML
# NEO_LISTEN_PORT=20333
RUST_LOG=info

# Optional: password for Grafana container in the sample compose file
GRAFANA_PASSWORD=admin
