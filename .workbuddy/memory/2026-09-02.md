# 2026-09-02 工作日志

## dBFT 完整本机回归与 ignored 覆盖清零

- 定位 `neo-consensus` 中唯一的 `#[ignore]`：`service::tests::core::test_message_deduplication`，忽略原因是「缺少为 `ConsensusPayload` 签名的测试辅助函数」。经核查，`create_validators_with_keys` 与 `sign_payload` 已在 `prepare.rs` / `recovery.rs` 中使用，因此属于**可补的测试 stub**，不是环境限制。
- 修复方式（仅改测试夹具，未改生产逻辑）：
  - 用 `create_validators_with_keys(7)` 生成验证人并取节点 0 私钥；
  - PrepareRequest 由 primary 发出（block 100、view 0、7 验证人 → `(100 - 0) % 7 == 2`），用 `keys[2]` 经 `sign_payload` 签名；
  - 断言改为严格语义：首次投递被接受、写入 seen cache、置 `prepare_request_received` 并产生 PrepareResponse 广播；重复投递返回 `Ok(())` 且不产生任何新事件；本节点 `prepare_responses` 只记录一次。
- 回归结果：`neo-consensus --lib` 完整套件 **110 passed / 0 failed / 0 ignored**（`CARGO_TARGET_DIR=target-audit-dbft-1`、`CARGO_INCREMENTAL=0`、`--jobs 1`）。此前记录的 `109 passed / 0 failed / 1 ignored` 已被取代。

## 环境坑：复用 target 目录导致链接失败

- 复用旧 `target-consensus-final` 时，链接器报 `ld.lld: failed to write output ... Permission denied` + `unable to remove file`。
- 已验证不是沙箱问题（关闭沙箱同样复现），也不是残留进程占用（进程列表为空）。
- 该 `.exe` 无法删除：bash `rm` 被安全删除包装器拦截（genie-trash 失败），PowerShell `Remove-Item -Force` 无效。
- **绕法：改用全新 `CARGO_TARGET_DIR`**（本次 `target-audit-dbft-1`），全量重编后链接成功。已记入 `overview.md` 环境说明。

## 代码核对（未改动）

- `lifecycle.rs` 消息路由顺序正确：block index 校验 → view 校验（ChangeView/RecoveryRequest/RecoveryMessage 允许跨 view）→ handler → 成功后才更新 `last_seen_messages` 与 seen cache。off-view 在路由阶段返回 `Ok(())` 且不标记 seen，因此 view 切换后可重放。
- `RecoveryMessage::validate()` 对 change_view / preparation / commit 三个 compact 集合均做 `validator_index` 去重，且在反序列化阶段抛出 `DuplicateValidator`，不污染 `is_recovering()` 状态。
- `prepare.rs` 中 PrepareRequest 时间戳校验：下界仅在 `prev_timestamp > 0` 时生效，`start()` 不设置该字段；上界按 `now + expected_block_time * factor` 计算，测试用小时间戳不受影响。

## runtime 最后 2 个 ignored 测试清零

用 `cargo test -- --ignored` 直接跑出真实原因（不靠猜）：

1. `relay_rejects_on_chain_conflict_with_same_sender`（`neo-core/src/ledger/blockchain/tests.rs`）
   - 原 ignore 原因「conflict stubs not being read correctly」是**误导**：真实失败点是 fixture 未初始化 GAS 总供应量，`persist_block` 执行 GAS burn 时抛 `Total supply cannot be negative`。
   - 修复：补一行既有 helper `seed_gas_total_supply(&mut store_cache, settings.initial_gas_distribution)`，与 fast-sync 测试同一模式；未改生产逻辑。
   - 恢复后真实验证 Neo N3 Conflicts 语义：同签名者的被冲突交易返回 `HasConflicts`，不同签名者的返回 `Succeed`。
2. `state_service_payload_ingests_into_shared_state_store`
   - 原 ignore 原因「State service initialization timing issue」已**过时**：直接执行即通过。
   - 为排除偶发，连续执行 5 次，5/5 稳定通过，故直接移除 ignore。

回归结果：

- `neo-core --features runtime --lib`：`740 passed / 0 failed / 0 ignored`（此前 `738 / 0 / 2`）。
- `neo-core --features runtime ledger::blockchain --lib`：`22 passed / 0 failed / 0 ignored`（此前 `20 / 0 / 2`）。

**方法论**：`#[ignore]` 上的文字说明经常与实际失败原因不符（本轮两个都是）。正确做法是先 `cargo test -- --ignored` 跑出真实错误，再决定是修 fixture、修实现还是删 ignore；且对标注「timing/flaky」的用例，恢复前必须重复执行确认稳定，不能直接删 ignore 了事。

## 收尾检查

- `cargo fmt --all -- --check`：通过。
- `git diff --check`：通过（仅既有 Windows LF/CRLF 转换提示）。
- 本轮代码差异：`neo-consensus/src/service/tests/core.rs`、`neo-core/src/ledger/blockchain/tests.rs` 与 `overview.md`；未创建 Git 提交（工作树仍混有大量历史/并行改动）。

## 仍未闭环

- 真实多节点 fast-sync/P2P 同步；
- Notary 从 `sendrawtransaction` 到 secondary payer 最终扣款的节点级链路；
- Docker 实际构建；Windows MSVC/RocksDB 原生构建；gnullvm 下 RocksDB `FILE_ID_INFO`/`FileIdInfo`；
- ~~`neo-core --features runtime --lib` 中 2 个既有 ignored 测试~~：已于本轮清零。

## dBFT future/off-view/Recovery 覆盖回归

- 精确回归全部通过：`prepare_response_with_future_view_can_be_reprocessed_after_view_change`、`off_view_commit_does_not_block_current_view_commit`、`recovery_message_rejects_duplicate_compact_validators_and_clears_flag`、`recovery_response_includes_compact_payloads`、`test_message_deduplication` 均为 `1 passed / 0 failed / 0 ignored`。
- 使用全新 `CARGO_TARGET_DIR=target-audit-dbft-view-4`、`CARGO_INCREMENTAL=0`、`--jobs 1`，首轮依赖构建和链接成功，未发生环境阻塞。
- 同一 target 随后执行 `neo-consensus --lib` 全量回归：`110 passed / 0 failed / 0 ignored`。覆盖结论：future-view 消息可在切 view 后重放；off-view Commit 不占用当前 view 状态；Recovery compact 重复 validator 被拒绝且 `is_recovering` 清理；seen-cache 仅对 handler 成功消息去重。未修改生产代码。

当前仍不能宣称项目已达到 100% Neo N3 完整正确实现。

## Notary/RPC 回归收尾

- `neo-core --test notary_contract_tests -- --nocapture` 使用 `target-notary-fix` 稳定完成：`22 passed / 0 failed / 0 ignored`。输出中的 ExecutionEngine fault 是测试主动覆盖的负向调用，最终断言全部通过。
- `neo-rpc --features server --lib` 使用 `target-audit-notary-rpc-2`，按实际模块路径串行执行三项 handler 测试，均为 `1 passed / 0 failed / 0 ignored`：有效交易入池、无 Notary deposit 拒绝、有效区块提交。
- 直接按函数名过滤时曾出现 `running 0 tests`，原因是忽略了 `server::rpc_server_node::tests::` 模块前缀；该结果明确不计为通过。
- 首次新 target 的 RPC 依赖编译被环境 `SIGTERM` 中断，未进入断言；清理短暂残留 Cargo 锁后复用已编译 target 完成验证。
- 新增 `outputs/notary-rpc-rerun-2026-09-02.md` 并更新 `overview.md`。当前仍未形成真实 HTTP/节点级 `sendrawtransaction -> 区块持久化 -> Notary::on_persist -> secondary payer deposit 扣款 -> RPC 查询余额` 闭环，原因是本地 consensus 关闭、无 wallet、无多节点 harness。

## Notary 节点级 roundtrip 闭环 + neo-storage find 生产缺陷修复

- **节点级链路首次闭环**：`notary_node_level_deposit_deduction_roundtrip`（neo-rpc，纯 RPC handler 驱动）通过。链路 = designate tx + deposit tx 经 sendrawtransaction 入池 → submitblock 同步持久化（GAS.transfer → Notary.onNEP17Payment 建 deposit 10 GAS）→ NotaryAssisted tx 入池 → block 2 持久化 → on_persist 从 payer deposit 扣 2 GAS。四项 RPC 测试最终 `4 passed / 0 failed`（target-audit-notary-rpc-3）。
- **测试侧修复**：roundtrip 的 deposit_script 上一轮 Edit 误删内层 `PACK`，导致 PACK count `Integer(2)` 被当 data 传给 transfer（trace `arg[3]=0x210102`）；已恢复并加注释。trace 中 `to=0x3bec3531...` 与 `Notary(0xc1e14f19...)` 为同一 hash 的 LE 字节/BE 显示两种形式，**不存在 hash 双方案问题**。
- **生产缺陷（已修复，neo-storage）**：memory provider `find` 前缀语义错误——`MemorySnapshot::find` 与 `MemoryStore::find(Vec<u8>)` 的 Backward 分支 `range(..=prefix)` 数学上不可能返回任何前缀匹配键（前缀键恒大于 prefix），Forward 分支不做前缀过滤返回超集。触发链：on_persist → get_notary_nodes → find_designation_bytes(Backward) → 空 → `No notary nodes designated` → 整块持久化失败 → submitblock `-500 Invalid`。RocksDB provider 用专用 `reverse_prefix_iterator` 语义正确，仅 memory 系列有缺陷。两处已改为过滤式实现（starts_with + Backward reverse）。
- **回归证据**：neo-storage `160 passed / 0 failed`；neo-rpc 四项 `4 passed / 0 failed`；neo-core `--features runtime --lib` 全量回归另行执行确认。
- **命令坑**：cargo test 多个过滤子串必须放 `--` 之后，当位置参数会 `unexpected argument`（EXIT=1 是命令错误非测试失败）。trace 的 LE 字节 hex 与 `to_string()` BE 显示差异会制造"两个 hash"的假象，比对 hash 时必须统一编码方向。

## neo-rpc 全量回归失败因果判定与清零（13 → 0）

- **首次 neo-rpc 全量（--features server --lib，567 项）暴露 13 个失败**，此前从未全量跑过（历史仅定向 4 项），无基线。四组归因：
  - **A 组（5 个，fixture 既有 bug）**：`store_block` 写 block_hash 键用 `index.to_le_bytes()`，生产 `block_hash_storage_key` 用 **BE**（对齐 C# 键序）。index=1 键不匹配 → getblock by index / getblocksysfee / verbose confirmations / raw tx blockhash 报 `-101 Unknown block`；index=0 时 LE==BE 故 genesis 测试碰巧通过。修 fixture 为 `to_be_bytes()`。
  - **B 组（5 个，测试假设错误）**：`ProtocolSettings::default()` 的 `standby_committee: Vec::new()`（protocol_settings.rs:173），5 个 candidates/validators 测试在 fixture 阶段 `standby_committee.first().expect("committee")` panic。新增 `settings_with_standby_committee(n)` helper（KeyPair::from_private_key([0x50+i;32])）注入非空 standby。
  - **C 组（1 个，同 standby 根因）**：`get_committee_returns_snapshot_members` 用空 standby 调 `store_committee` → 写入空数组 → 生产 `decode_committee_stack_value` 对空缓存报 "committee cache empty" → handler "committee not available"。改用 helper(1)。
  - **D 组（2 个，find 修复真实波及→生产修正）**：`find_range`（neo-core/tokens_tracker/extensions.rs）把 `find(Some(start_key), Forward)` 当范围查询用——旧 Forward 超集语义下 `range(start_key..)` 有序返回下界之后的键 + `>end_key` break 恰好构成 range；新前缀语义下 start_key 含精确时间戳（t1-1），无键以其开头 → 返回空 → nep11/nep17 transfers desc 断言 `sent.len 0 != 2`。
- **find_range 生产修复**：取 start_key/end_key 最长公共前缀做 find（调用方场景 = [prefix+user]），显式 `key < start_key continue` / `> end_key break` 过滤；注释说明旧超集依赖。签名不变，两处 handler 调用与测试直调自动恢复。
- **回归结果**：neo-rpc 全量 `562 passed / 0 failed / 5 ignored`（EXIT=0）；neo-core --lib `584 passed / 0 failed`；fmt 全绿（EXIT=0）。
- **基线反证受阻**：`git stash push` 失败 EXIT=128（`fatal: 8a2c224... is not a valid object`）；进而发现 **git 仓库对象库大面积损坏**——`.git/refs/heads/` 松散引用目录被清空、pack 目录只剩 idx 无 .pack 数据文件、reflog 链上全部 commit 对象（含远端 tip 430ca408）`cat-file` 全部无效。**工作树完好**（3 个修复文件与 /tmp/find-fix-backup 逐字节一致，WORKTREE_INTACT）。"修复前 nep11/17 通过"由旧实现字面语义机械推导（Forward `range(prefix..)` 超集 + 旧 find_range break 截断 = 精确 range，t1/t2 ∈ [t1-1, now+1] 必返回）。
- **git 重建建议（未执行，交用户）**：备份工作树改动清单 → 重新 clone origin → 切 protocol-v3.10.1-compliance（远端 tip 430ca408）→ 迁移未提交修改与 outputs/.workbuddy 未跟踪目录。本地 reflog 最后位置 1c85a649（"build: align Docker and Windows MSVC configuration"）若未推送则对象不可恢复。
- **教训：并行 Edit 同一文件会相互覆盖**——6 个 Edit 同时提交仅 2 处调用 + helper 幸存，3 处修改静默丢失（测试仍 panic 同一 expect 才暴露）。对同一文件的多个 Edit 必须串行执行。
- **命令坑**：`cargo test ... | tail` 管道后 `$?` 是 tail 的退出码，cargo 失败也报 EXIT=0——必须重定向到文件再 `echo EXIT=$?`。

## git 仓库重建与 stash 二度损坏实录

- **重建成功**：旧 `.git` 改名 `.git-corrupted-20260902` 留档 → `git clone --no-checkout https://github.com/r3e-network/neo-rs.git` → 换入新 `.git` → 本地 `core.autocrlf=false`（覆盖全局 true，避免行尾噪音）→ 手工建本地分支 `protocol-v3.10.1-compliance` = 430ca408（clone 只为默认分支建本地分支，symbolic-ref 指向不存在的本地分支会得到 unborn HEAD + 空	index，需 `git update-ref` 指向远端 tip）→ `git reset -q` 重建 index → `git branch --set-upstream-to` → 备份目录写入 `.git/info/exclude`（本地排除，不污染 tracked .gitignore）。最终 `git log`（430ca408 "release: v0.15.0"）与 status（954 条 = 工作树 vs 远端 tip 全差）正常。
- **注意**：工作树 diff 基线从丢失的本地提交 1c85a649 变为远端 tip 430ca408，审计改动与 1c85a649 增量合并在同一条未提交 diff 里，属预期。
- **stash 二度损坏（决定性实证）**：为补基线反证在新仓库执行 `git stash push`（**已关沙箱**），再次删光 `refs/` 整个目录 → `fatal: not a git repository`，后台任务卡死 21 分钟被手动终止。pack 对象库完好（pack-9869a751*.pack 在），三个修复文件未被回滚（备份逐字节校验一致）。按手工手法 `mkdir -p .git/refs/heads && printf '430ca408...\n' > refs/heads/...` 重建，`git reset -q` 后全恢复，nep11/nep17 两测试复验 `2 passed / 0 failed`。
- **结论升级：本环境 `git stash` 无论沙箱与否都会摧毁 refs 事务，永久禁用**；需要临时回滚用文件级备份（cp + diff 校验）替代。基线反证（stash 方案）就此放弃，~~维持字面语义推导定性~~已改由文件级交换完成实证（见下节）。
- 后台任务无流式输出，20+ 分钟无 stdout 时应立即开只读诊断（git HEAD/refs/进程表），而不是继续盲等。

## 基线反证实证完成（文件级交换）+ integration 波及面回归启动

- rq1gk1（5m53s，target-audit-notary-rpc-3）：`git show HEAD:` 只读提取 3 个修复前文件（memory_snapshot/memory_store/extensions）→ sanity（旧版本 `scan_prefix`/`starts_with` 计数 0/0）→ cp 换入工作树 → **BASELINE_EXIT=0，2 passed（565 filtered）** → 从 /tmp/find-fix-backup 恢复修复版 → `diff -q` RESTORE_OK → **POSTFIX_EXIT=0，2 passed**。全程零 git 状态变更。
- D 组因果链三态实证闭环：旧 provider+旧 find_range 通过（实证）/ 新 provider+旧 find_range 失败（首轮 13 失败回归现场）/ 新 provider+新 find_range 通过（全量 562/0/0 + 复验 2/2）。工作树已恢复修复版（逐字节一致）。
- 波及面回归进行中：`cargo test --jobs 1 -p neo-tests`（全部 11 个 `[[test]]` 目标，全新 `target-audit-integration-findreg`），对照历史基线 consensus_integration 19/0/0、end_to_end 7/0/0、p2p_message_exchange 5/0/0（均运行于 find 修复之前）。~~另发现 `tests/tests/persistence_boundary_tests.rs` 在磁盘上但未注册进 tests/Cargo.toml 的 `[[test]]`，不会编译运行~~（**更正**：cargo autotests 默认自动发现 `tests/*.rs`，该目标实际运行且 1/0/0 通过）。

## integration 波及面回归结果：3 基线对齐 + 幽灵 log 生产修复

- **完整矩阵**（--no-fail-fast）：三个历史基线全部对齐（consensus_integration 19/0/0、end_to_end 7/0/0、p2p 5/0/0）——find 修复对集成基线零波及。新增覆盖：chaos 7/0/0、contract_execution 12/0/1、e2e_transaction_flow 14/0/0、layer_boundary 11/0/0、state_integration 18/0/0、persistence_boundary 1/0/0。`block_persistence` 为 0/0/0（7 行空壳文件，neo-chain 删除后测试已移除——0 tests 不能计为通过，已记录）。
- **no_local_neo_vm_dependency 2 failed** → 文件级交换实验（dvJ5zK，2m53s）实证：旧 find 文件下同样 2 failed——与 find 修复无关，属工作树既有失败。
- **失败 #1 根因（探针实验定案）**：Runtime.Log 路由本身正确（每 syscall 恰一条）；多出的是幽灵 log `"Native contract PolicyContract initialization error: GetTime can only be called with Application trigger"`。链路：`ApplicationEngine::new` → state.rs:78 `register_native_contracts()` 急切初始化 → 无 persisting block 时 current_block_index()=0 → PolicyContract 创世 initialize → GetTime 因 persisting_block None 报错（文案误导）→ witness_and_misc.rs 把错误包装成 LogEventArgs 污染 `logs()`。**git diff 实证为远端 tip 既有行为**（两处代码均不在工作树 diff）；C# 偏差（C# Log 事件仅由 Runtime.Log syscall 触发）。
- **生产修复**：witness_and_misc.rs 初始化失败改 `tracing::warn!`（不再 emit_log_event），C# 对齐理由入注释。旁证：`runtime_syscall_tests.rs` 两处同样断言受害；`blockchain_application_executed.rs:53` 把 logs() 写入持久化 payload（幽灵可触达真实应用日志）。全仓 grep 无测试依赖幽灵 log。
- **失败 #2**：源码扫描守卫要求 block/verification.rs 含 `verify_state_dependent_at_height(`——但该文件已按早前审计决策（C# Block.Verify 委托 Header.Verify）重构，条款永不可能通过。测试侧现代化：保留高度显式 API 委托两条款，第三条款改为禁止块级非高度显式校验（`!contains("verify_state_dependent(")`），守卫意图完整保留。
- **修复后回归矩阵全绿（x7M5IK，9m44s，全部 EXIT=0）**：neo-rpc server lib **562/0/5**（基线一致）；neo-core runtime lib **740/0/0**（基线一致）；runtime_syscall_tests **32/0/0**（首次真实回归，两处幽灵病灶治愈）；no_local 全目标 **121/0/0**（2 失败清零）；fmt 全绿。本轮审计链条收口：find 修复基线反证实证闭环 + 集成波及面回归 + 幽灵 log 生产修复 + 守卫现代化，全部落盘。

## neo-rpc ignored 清零（5/5）+ 下游 sweep 全绿

- 5 个从未检视的 `#[ignore]` 实跑（--ignored）：3 过 2 败。3 个理由过时直接摘除（get_block_hash 疑似被 find 修复顺带治愈、submit_block ×2）；2 个失败均为**测试侧问题**，零生产改动：
  - diagnostics：断言 `!storage_changes.is_empty()` 前提错误——C# `GetInvokeResult` 的 storagechanges = `Engine.Snapshot.GetChangeSet()`（仅本次执行未提交写集）；`Session::new`（session.rs:106-107）用含创世的 `store_cache().data_cache().clone()` 建引擎，纯读脚本必为空 → 断言翻转 + C# 引注。
  - nep11 properties：夹具 `"0x0102"` 违反 C# `HexToBytes()` 纯 hex 约定（响应侧 `ToHexString()` 输出闭环）→ 改 `"0102"`。
- **C# 逐字证据反转 GetTime 修复候选**：master/master-n3 双分支 `GetTime()` 均在 `PersistingBlock is null` 时抛 `InvalidOperationException("GetTime can only be called with Application trigger.")`，与本仓 `current_block_timestamp()` 逐字一致——`PersistingBlock?.Timestamp ?? 0` 背景知识说法作废，本仓行为即 C# 对齐。
- 回归（6OcNoX，3m23s）：neo-rpc --features server --lib **567/0/0**（ignored 清零）+ fmt EXIT=0。
- 下游 sweep（1n9vuK，5m19s）：neo-consensus 110/0/0、neo-p2p 19/0/0（历史首次）、neo-telemetry 21/0/0（历史首次）——find + 幽灵 log 修复零波及。
- **C# 仓库定位**：RpcServer/TokensTracker 插件权威位置是 neo-project/neo-modules（neo 主仓已无 src/Plugins）；neo 主仓默认分支 master-n3 是 N3 主线，master 是下一代。

## 全仓 ignore 清点（21 → 16 合理保留）+ 死目标 local_node_relay_tests 复活

- 全仓 `#[ignore]` 清点 21 处：16 个数据门控合理保留（11 × mainnet_block_repro + 2 probe/stateroots + 2 compliance fixture，需本地主网数据/C# fixture，理由准确；`--no-run` 编译验证全部通过）；2 个真实检视清零；2 个是 Doc-tests 文档围栏（非债务）。
- **contract_execution `test_vm_invalid_opcode`**：实跑 1 passed（`interpret` 对 `[0xFF,0xFF,RET]` 正确 Err，C# NeoVM FAULT 对齐）→ 摘 ignore，全目标 13/0/0。
- **neo-io `test_write_float_compatibility`**：空壳测试（函数体全注释、零断言）；Neo 协议从不序列化浮点，neo-io 读写两侧均无 float 方法（无不对称）→ **删除**，不投机加 write_f32/f64 API。neo-io 全目标 235/0/0。
- **附带发现死目标**：`neo-core/tests/local_node_relay_tests.rs` 4 处 E0308（`VersionPayload::create` 首参已改 `u32` network magic，测试仍传 settings）——git status/diff 零差异证实为**远端 tip 430ca408 既有**，该目标自 tip 起从未编译运行。测试侧修复 4 处（`settings` → `settings.network`，对齐 p2p_message_tests.rs:203 已迁移调用点）→ 复活首跑 **11/0/0**。
- 回归全绿：contract_execution 13/0/0、neo-io 235/0/0、local_node_relay_tests 11/0/0、fmt -p neo-io/neo-core/neo-tests ×3 全绿。
- 经验：审计"未跑过的测试"时，`cargo test --no-run` 全目标编译检查能暴露死目标（编译破坏 = 零覆盖），其价值等同跑测试本身。

## 全工作区测试目标清扫收口（全部 EXIT=0）

- 清扫矩阵（`--tests --no-fail-fast`，迭代三轮）：neo-vm 122、neo-json 53、neo-crypto 144、neo-primitives 286、neo-p2p 23（五 crate 历史首次全绿）、dbft 110、telemetry 21、neo-io 235、**neo-rpc 583**（lib 567 + 集成 16：rate_limiter_governor 8、vmstate 1[BE 夹具修复验证]、handler_registration 2 / validate_address 1 / ws_events 4 三目标首跑）、**neo-core 1495/0**（lib 740 + 86 集成目标，100 个 test result 全 ok）。
- **rustc ICE 绕过**：第二轮 neo-rpc 清扫 `rustc_metadata\src\rmeta\encoder.rs:2447:51` panic，零测试输出——换全新 CARGO_TARGET_DIR（target-audit-rpc3）即愈；与链接 Permission denied 同策略，不计为代码失败。
- **第二个死目标复活**：task_manager_restart_tests 2 处 E0308（`VersionPayload::create` 签名迁移同源）→ `ProtocolSettings::default().network` ×2 → 5/0/0。
- **native_token_tests 3 失败定案为测试侧问题**：Rust `runtime_notify` 按 `Hardfork::HfBasilisk` 门控（legacy 无校验 / basilisk 按 manifest ABI events 白名单 + 参数数量/类型校验）；C# master-n3 `ApplicationEngine.Runtime.RuntimeNotify` **逐字同语义**（`IsHardforkEnabled(HF_Basilisk)` → `RuntimeNotifyV1` 不校验；启用后 ordinal 匹配缺失抛 `Event \`X\` does not exist.`）——Rust 是忠实 parity。根因是测试部署的合约 manifest 事件列表为空却 Notify Payment/PaymentData，且引擎用 `ProtocolSettings::default()`（Basilisk 默认启用）。修复：3 个 manifest 补 `ContractEventDescriptor`（Payment ×Hash160、PaymentData ×Any）→ 12/0/0 → 全量复跑 CORE3_EXIT=0 / 1495 passed 复确认。
- **背景知识修正**：C# Runtime.Notify 在 HF_Basilisk 后确实按 manifest 校验事件名（此前以为从不校验）；Basilisk 前 V1 路径才不校验。
- 经验补充：neo-core2 日志 2 条 `^error` 甄别 = cargo 汇总行（非新失败）；`grep -cE "^error"` 会把 cargo 的 `error: test failed` / `error: N target failed` 汇总也算进去，先看上下文再定性。

## 清扫矩阵完备化：剩余 4 crate 全绿 + benches 编译通过 + fuzz 环境阻塞定性

- neo-storage 160/0/0（基线一致）、neo-hsm 6/0/0、neo-tee 54/0/0、neo-config 14/0/0——四 crate 无 tests/ 目录，覆盖全在 lib；全部 EXIT=0。工作区 14 crate 清扫矩阵完备。
- benches-package **不是工作区成员**：`cargo bench --no-run -p benches-package` 报 "did not match any packages"（EXIT=101），须 `--manifest-path`；正确姿势下 3 个 bench 目标 release 编译全通过（EXIT=0，7m30s），非死目标。
- fuzz 独立 crate `cargo check` 失败定性为**环境级阻塞**：libfuzzer-sys 0.4.10 构建脚本编译 libFuzzer C++ 源（FuzzerExtFunctionsWindows.cpp）时 clang++ 失败——libFuzzer 不支持 Windows-gnu/gnullvm 工具链，与 neo-node RocksDB FILE_ID_INFO 同类，非代码缺陷，不计为测试失败。
- 环境备注：共享 target 目录顺序跑 4 个小 crate 摊薄依赖编译（一个全新 dir ~10m22s 完成 4 crate + benches 首建），比每 crate 独立目录省 3 次全量 neo-core 重编。

## neo-tests 全绿 + 双重门控精化 + rocksdb 定案 + neo-hsm 门控死代码修复

- neo-tests 包（工作区第 15 个成员）全目标 TESTS_EXIT=0，与历史基线全对齐（no_local 121、consensus_integration 19、contract_execution 13 等 11 目标）。工作区 17 成员中 15 个全绿。
- **数据门控 ignore 真实机制 = 双重门控**：14/16 个（11 mainnet repro + 3 probes/stateroots）文件首行 `#![cfg(feature = "rocksdb")]`——默认构建下整个文件不编译（sweep 显示 0/0/0 连 ignored 都不计），叠加测试级 #[ignore]；仅 block_validation_compliance 的 2 个是纯 #[ignore]。
- **rocksdb 激活定案**：librocksdb-sys（无 bundled）fallback 编译 vendored C++，env_win.cc 处 clang++ 4 errors（gnullvm 缺 FILE_ID_INFO，与 neo-node 同源）；两次尝试同点失败。14 个门控测试文件本机 definitively 不可激活。
- **neo-hsm feature 门控死代码修复（生产，完整证据链）**：`--features ledger,pkcs11` 12 errors 全为生产 lib 代码（git diff 仅 import 排序 → tip 既有）。根因：ledger `Option<&str>` 双重 unwrap_or 误用 ×2、pkcs11 `decode_der_octet_string` 省略生命周期错误、cryptoki FFI 字段不满足 `HsmSigner: Send + Sync` ×7。修复：newtype `ThreadSafePkcs11`/`ThreadSafeSession` + unsafe impl（论证：OsThreads = CKF_OS_LOCKING_OK，PKCS#11 v2.40 §2.2.2 线程安全 + RwLock 后置访问）、显式 'a 生命周期、7 处使用点适配。验证：feature 构建编译通过 + 6/0/0，默认构建零影响。
- **基准方法教训**：`grep -rch P | wc -l` 把 `-c` 的按文件计数行数当匹配总数——"15 个 #[test]"实为误读；实际 6 个测试函数（3 #[test] + 3 #[tokio::test]），ledger/pkcs11 模块零测试代码。正确姿势 `grep -rho P | wc -l`。
- feature 门控代码 = 死目标发现模式又一变种：`--features` 激活编译能暴露"默认构建永不编译"的代码。
