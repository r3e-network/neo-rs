# 2026-08-30 项目工作日志

- 更新 `overview.md`，同步本轮 Neo N3 协议审计的最新状态：dBFT Primary timer/one-shot、seen-cache 路由隔离、stale ChangeView/ off-view Commit 隔离、Recovery compact 去重和 `is_recovering` 清理；MemoryPool 37 passed；StateService 63 passed；Blockchain actor failure/retry；Notary RPC/Core 定向验证；Docker、Windows MSVC/RocksDB、runtime 集成和完整节点级链路仍未验证。
- 明确记录：主会话本机最新完整 `neo-consensus --lib` 在 Windows gnullvm linker 写入测试可执行文件时因 `Permission denied` 阻断，未进入测试断言；代理独立环境本轮完整套件报告 `109 passed / 0 failed / 1 ignored`。因此当前不能宣称项目达到 100% Neo N3 完整正确实现。
- `overview.md` 的工作树状态为未跟踪文件，已完成内容复核且 `git diff --check` 通过；未执行破坏性 Git 操作。
- 最终核对发现 `neo-node` 的 consensus wiring 原先未把 `ProtocolSettings.max_transactions_per_block` 注入 `ConsensusContext`；新增 `ConsensusService::set_max_transactions_per_block`，并在恢复、复用、新建 round 三条路径注入。`cargo fmt --all -- --check` 通过；`cargo check -p neo-node` 仍在 bundled RocksDB 的 LLVM-MinGW 编译阶段因 `FILE_ID_INFO`/`FileIdInfo` 缺失失败，属于当前 Windows 工具链/SDK 环境限制。
- 修复 `neo-core/src/state_service/commit_handlers.rs` 测试时序：`Committing` 仅 stage state root，`Committed` 才执行底层 backend commit；更新 `StopPlugin`/`Continue` 测试以覆盖真实事件顺序，并验证 staging 后 local root 不提前推进。StateService commit handler 定向测试最终为 `4 passed / 0 failed`。
- 完整 `neo-core --features runtime --lib` 回归使用独立 target、关闭 incremental、单并发执行，结果为 `738 passed / 0 failed / 2 ignored`。剩余 2 项 ignored 为既有覆盖缺口；本轮未新增 runtime 失败。
- 修复 `tests/tests/consensus_integration_tests.rs` 中四个依赖 Primary proposal 的测试时序：`ConsensusService::start()` 后显式调用 `on_timer_tick(16_001)`，与生产初始 timer gate 保持一致。`consensus_integration_tests` 真实本机回归为 `18 passed / 0 failed / 1 ignored`。
- 真实本机回归 `end_to_end_tests` 为 `7 passed / 0 failed`，`p2p_message_exchange` 为 `5 passed / 0 failed`。这些测试分别覆盖状态/基础端到端和 P2P 消息层，不等价于真实多节点同步。
- 使用 `config/testnet.toml` 执行 `neo-node --check-config`、`--check-all` 成功；使用 `config/local.toml` 启动单节点 RPC 冒烟成功，绑定 `127.0.0.1:30332`，`getblockcount` 返回 `1`，`getrawmempool` 返回 `[]`。该结果仅证明单节点初始化和只读 RPC 可用，未证明 dBFT、fast-sync 或真实 P2P 同步。
- 串行重试 `neo-core --features runtime --test persistence_fast_sync_handler_tests` 成功，结果 `2 passed / 0 failed`，覆盖 fast-sync handler 数据接收和 committing failure 在 commit 前阻断。`neo-core --test notary_contract_tests` 成功，结果 `22 passed / 0 failed`，覆盖 Notary native contract、deposit/withdraw 和 sponsored fee 扣款。
- `neo-node --test block_assembly_test` 未进入测试断言，Windows gnullvm bundled RocksDB 编译因 `FILE_ID_INFO`/`FileIdInfo` 缺失失败；Docker CLI 和 MSVC/clang-cl/lib.exe 仍不可用。当前仍不能宣称项目达到 100% Neo N3 完整正确实现。
- 恢复 `tests/tests/consensus_integration_tests.rs` 的 wrong-view 覆盖：当前 view 外的 `PrepareResponse` 按生产语义应返回 `Ok(())`、不修改 `prepare_responses`、`prepare_response_hashes` 或 `last_seen_messages`，并保留重复重传机会。修正断言后真实本机回归为 `19 passed / 0 failed / 0 ignored`；`cargo fmt --all -- --check` 通过。
- 2026-09-01 补充独立 target 串行回归：`neo-core --features runtime --lib state_service` 为 `68 passed / 0 failed / 1 ignored`；`ledger::blockchain` 为 `20 passed / 0 failed / 2 ignored`；`neo-primitives::unhandled_exception_policy` 为 `4 passed / 0 failed`。确认 StateService staged commit/失败清理、Blockchain actor 同区块重试和 `StopPlugin`/`Continue` 语义未出现新失败；新增结果摘要保存于 `outputs/audit-rerun-2026-08-30.md`。当前仍不能宣称项目达到 100% Neo N3 完整正确实现。
