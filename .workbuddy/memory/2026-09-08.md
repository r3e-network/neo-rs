# 2026-09-08 工作日志

## v0.15.0→v0.17.0 新版本审计（团队 software-neo-audit-v017）

- 范围 `430ca408..HEAD`（737 文件，实质变更约 1.0-1.3 万行，46.3% 空白噪音）；排除工作树 58 个未提交文件。
- 架构师测绘：6 个 P0 + T01-T09 分解（`outputs/audit-v017-scoping-2026-09-08.md`）；校正：a3231470 不是噪音来源（仅 4 文件 81 行），主体噪音在 0819e70e。
- T08（工程师）：R01-R21 对账 0 缺失但 9 项无回归测试；15 条声明不符（D-01 "protocol compliance validated" 无支撑、D-15 默认 feature 实测 586 非 741）；确证 D-04：`memory_pool/mod.rs:351` 冲突移除原因 Conflict→CapacityExceeded 偶发漂移。
- QA 与工程师成员先后因平台 429/401 额度中断，T01/T02 由主会话接手完成（报告已注明）。
- T01：15/15 crate `--no-run` EXIT=0（PASS）。**E1 表述修正**：neo-node 默认 features=[] 不拉 RocksDB；`--features full` 实测 EXIT=101 复现 FILE_ID_INFO（E1/E3 维持）；E2 fuzz 复现。首轮三条"失败"全是命令错误（包名 dbft 不存在→neo-consensus；IFS='|' 污染分词吃掉 --features）。
- T02 证伪三项 P0：P0-1 指令上限移除=C# 对齐（C# ExecutionEngineLimits 无指令上限；RPC max_gas_invoke 10 GAS 与 C# 一致）；P0-2 双路径 SHL/SHR 分歧不可达（external_vm.rs:298-302 pre-Gorgon 守卫回退 jump table）；P0-3 Block::verify=C# 逐字对齐（C# Block.Verify 就是 Header.Verify 纯委托，Merkle/去重在反序列化）。
- **M-01 端序风险正式核销**：C# NeoToken.GetSortedGasRecords 用 ReadUInt32BigEndian、ContractManagement.GetContractHashes 用 ReadInt32BigEndian 读回自己的键——两端均大端；GasPerBlock 前缀 29 在 C# 同样属于 NeoToken（非 PolicyContract）；legacy LE 回退是无害清扫 shim。
- **新发现 F-NEW-1（P1）**：HASKEY 上界——C# 对 index>=MaxItemSize 抛异常，Rust semantics/collections.rs:234-249 无上界检查返回 false，可构造执行分歧。
- P0-4 值半边实测闭环：verify-protocol-presets.py 对 mainnet(860833102)+testnet T5 活节点 38 项 ALL MATCH EXIT=0（mainnet faun=8800000/testnet 12960000 各段独立确认）；CI 无闸门维持。
- P0-5 dBFT 定时器重构未验证（缺 C# ConsensusService 逐行对照）——本轮最大剩余缺口。
- T06 全量测试矩阵、T07 安全专项未执行（额度中断）。最终报告：`outputs/audit-v017-final-report.md`。
- 测试：neo-vm lib 128/0/0、neo-consensus lib 114/0/0（基线分别 128/110，均无回归）。

## 修复落地（同日追加）

- **F-NEW-1 已修复**：`neo-vm/src/semantics/collections.rs` 新增 `has_key_index()`（负索引 + `index >= MAX_ITEM_SIZE` 均 FAULT，注释引 C# JumpTable.HasKey）+ 测试 `has_key_index_beyond_max_item_size_faults`（3 断言，含边界 -1 与 MAX-1 仍 Ok(false)）。
- **D-04 已修复**：`memory_pool/mod.rs:351` 恢复 `TransactionRemovalReason::Conflict` + 注释；纠正固化漂移的测试 `conflict_replacement_emits_capacity_exceeded_reason` → `conflict_replacement_emits_conflict_reason`。
- 验证：neo-vm lib **129/0/0**、neo-core lib（--features runtime）**742/0/0**、`cargo fmt -p neo-vm --check` 绿、我改的 3 个文件 rustfmt --check 绿。
- **fmt 基线观察**：HEAD 的提交文件存在 fmt 偏差（connection.rs/block_validation.rs 换行风格、ledger_contract/mod.rs），未提交的 upnp.rs/contracts.rs 也有——`cargo fmt --all --check` 在 v0.17.0 不再全绿；未顺手改无关文件。
- 遗留待办：#3 CI 挂对账脚本/README 声明修正/run_all() stub；#4 P0-5 dBFT C# 对照；T06/T07 全量矩阵。

## 治理项 + P0-5 + T06 收官（同日二追加）

- **治理项落地**：ci.yml `protocol-consistency-goldens` 作业新增 `verify-protocol-presets.py` 对账步骤；README 修正 4 处声明；`test_harness.rs` 的 `run_all()` 从 Not-implemented stub 重写为真实逐向量校验（反序列化→hash→size→merkle/时间戳/交易数→字节级 round-trip）+ 防 stub 回归测试。注意 UInt256 无 `{:#x}` Display，用 `to_hex_string()`。
- **P0-5 定案（C# neo-modules/src/DBFTPlugin/Consensus 逐字对照）**：M=N-(N-1)/3、主节点发 PrepareRequest 后定时器公式 `ms<<(v+1)-(v==0?ms:0)`（怪公式逐字一致）、backup `<<(v+1)`、ExtendTimerByFactor 公式+守卫（`!ViewChanging&&!CommitSent&&nextDelay>0`）+因子（PrepareResponse→2、Commit→4）全对齐；唯一差异是主节点 view0 启动定时器（C# 扣除距上块已流逝时间 vs Rust 固定整块）——活性节奏差异非安全。**C# N3 后期 dBFT 在 neo-modules 的 DBFTPlugin（core 无 Consensus 目录）**。
- **T06 完成**：15 crate 全量。12 全绿；neo-rpc 4 失败+neo-tests 5 失败**全部归因未提交 WIP**（限流器 .max(1)/CORS 翻转断言未同步；payload 签名改 fail-closed 使零私钥测试 panic；has_enough_prepare_responses 重写为 hash-匹配超额加固破坏 2 个 R01 测试——HEAD 实现逐断言推演通过）。HEAD 零失败。
- **F-NEW-3（P2，已纠正）**：`test_verify_state_independent_invalid_invocation_script` 期望与 C# 相悖——C# VerifyStateIndependent 只内联单签/多签快速分支，**不匹配即静默跳过、不调 VerifyWitness**；Rust HEAD"NonStandard 跳过"与 C# 一致。我曾误改 verification.rs（NonStandard 走 VM）后依 C# 逐字证据**回滚**，测试改名 `..._skips_non_standard_invocation` 断言 Succeed。该测试 0819e70e 入库即死（runtime 门控 CI 不跑，潜伏 40 天）。**教训：测试失败≠代码错，先取 C# 逐字证据再动手。**
- **修复后 neo-core 全目标 EXIT=0（101 个测试二进制全 ok）**。
- **事故披露**：`cargo fmt -p neo-core`（crate 级）意外重排 WIP 文件 upnp.rs（数处 assert 拆行）/contracts.rs（一处 use 排序）的格式部分，无法无损分离；干净文件未被动。**教训：含 WIP 的 crate 禁用 crate 级 cargo fmt，只对自改文件用 rustfmt 单文件。**
- 剩余：T07 安全专项（额度）、rocksdb 门控测试激活（E1b 阻塞）、WIP 作者同步 9 个测试、CI runtime-lane（F-NEW-3 根因）。
