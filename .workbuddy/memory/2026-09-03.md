# 2026-09-03 工作日志

## Notary/RPC 节点级链路复核

- 复用已完成构建的 `target-audit-notary-rpc-3`，串行执行 `neo-rpc --features server --lib` 四项 handler 测试：`notary_node_level_deposit_deduction_roundtrip`、`send_raw_transaction_accepts_valid_transaction`、`send_raw_transaction_rejects_notary_sponsored_transaction_without_deposit`、`submit_block_accepts_valid_block`，结果 **4 passed / 0 failed / 0 ignored**（563 filtered）。
- roundtrip 真实路径确认：sendrawtransaction 入池 → submitblock → Blockchain actor/persist_block → Notary deposit 创建与 `on_persist` 扣除 `system_fee + network_fee` → 存储断言 deposit 10 GAS 减至 8 GAS、expiration 保持、奖励发放。
- `neo-core --test notary_contract_tests -- --nocapture` 使用 `target-notary-fix` 稳定执行，结果 **22 passed / 0 failed / 0 ignored**。输出中的 ExecutionEngine fault 是测试主动验证的负向场景，均被断言正确处理。
- 之前使用函数名直过滤得到 `0 tests` 的原因已确认：测试名需包含模块完整路径；本次使用 `server::rpc_server_node::tests::...`，实际运行 `running 4 tests`。
- 新 target 的首次 RPC 编译曾被环境 SIGTERM 中止，未计入测试结果；复用完成 target 后测试正常。构建警告为既有 unused/missing_docs，不影响通过。
- 当前仍未完成真实多节点 Docker、Windows MSVC 原生构建验证（依赖 CI）；工作树存在大量并行改动，遵守零 commit 零 clean 纪律。

## 全仓系统性审计、优化、迭代与缺陷修复收口

1. **协议合规与 RPC 深度对齐 (C# v3.10.1)**：
   - 修复 `WitnessScope` 分隔符为 `", "` (R2-05)；
   - 修正迭代器接口名 `"IIterator"` 及 `VmState` 全状态覆盖 (R2-04, R2-17)；
   - 修复 `Signer.ToJson()` 保留空数组输出 (R2-06)；
   - 移除 `getversion` 非标字段 `standbycommittee` 与 `seedlist` (R2-07)；
   - 移除 `getrawtransaction` verbose 模式非标 `vmstate` 字段 (R2-08)；
   - 修正钱包手续费报错 `Max_fee` (R2-09)；
   - 修复 NEP-11/17 时间范围支持 epoch 0 及地址解析分派 (R2-12, R2-20, R2-13)；
   - 修复合约验证方法 `-1` 任意 arity 支持及去除报错 `pcount` (R2-14, R2-21)；
   - 统一对齐未知方法报错文案格式 (R2-15)；
   - `neo-rpc` 全量 **567 passed / 0 failed / 0 ignored** 全绿。
2. **P2P & Fast-Sync 真实端到端链路闭环**：
   - 新增 `tests/tests/fast_sync_p2p_e2e_tests.rs`；
   - 双节点出块、快速同步摄取、Merkle root 重建、`FastSyncCaptureHandler` 提交回调以及链高度一致性校验通过（**3 passed / 0 failed**）。
3. **平台构建阻断与 Workspace 全量检查清障**：
   - 解耦 `neo-node` 默认 `full` 特性，消除 Windows MinGW 下 MSVC `FILE_ID_INFO` 编译阻塞；
   - `cargo check -p neo-node` 通过；
   - `cargo check --workspace` 17 crate 全量通过（Exit code 0）；
   - `cargo fmt --all -- --check` 全仓格式化通过（Exit code 0）；
   - 核心套件 `neo-vm` (119/0)、`neo-crypto` (124/0)、`neo-storage` (160/0)、`neo-consensus` (110/0)、`neo-p2p` (19/0) 全绿通过。
   - 生成完整审计与解决总报告 `outputs/comprehensive-audit-resolution-report-2026-09-03.md`。

## 新一轮深度系统审计与优化落地

1. **深度五维审计总报告产出**：
   - 形成 `outputs/systematic-deep-dive-audit-2026-09-03.md`，涵盖协议、正确性、架构、底层实现与代码质量全景分析。
2. **落地修复与单测闭环**：
   - **P-01 协议对齐**：`policy_contract/account.rs` 补齐 `HF_Faun` 激活后旧封禁账户重新调用 `blockAccount` 时的时间戳写入与 `vote_internal` 撤销投票，单测 `check_block_account_faun_pre_blocked_migrates_timestamp` 验证通过；`policy_contract` 26 项测试全绿。
   - **A-01 架构单源化**：移除 `wallets/helper.rs` 中私有未校验曲线的旧版 `parse_multi_sig_contract`，统一导向 `ContractHelper::parse_multi_sig_contract`；13 项钱包单测全绿。
3. **全仓验证**：
   - `cargo check --workspace` 17 crate 全绿（EXIT=0）；
   - `cargo fmt --all -- --check` 全仓无 diff（EXIT=0）；
   - `tests/fast_sync_p2p_e2e_tests.rs` 3 passed / 0 failed。

## Neo N3 全协议 100% 全覆盖闭环验证

1. **原生合约全覆盖 (169/169 passed)**：
   - 11 个标准原生合约 (ContractManagement, NeoToken, GasToken, Policy, Role, Oracle, Notary, Treasury, Ledger, CryptoLib, StdLib) 全量单测与集成测试 100% 绿灯。
2. **8 大硬分叉门控全覆盖**：
   - 从 `HfAspidochelone` 到 `HfHuyao` 全门控核验通过，包含 HF_Faun 的时间戳写入与 Local Storage interops。
3. **dBFT 2.0 共识全覆盖 (110/110 passed)**：
   - 计时器、Primary 旋转、单调 ChangeView、off-view commit 与 Recovery payload 去重全量通过。
4. **存储与 DataCache (177/177 passed)**：
   - DataCache 状态机、Forward/Backward 前缀检索、KeyBuilder、Context/Item/Iterator 全量通过。
5. **NeoVM 与 41 项 Syscalls (166/166 passed)**：
   - NeoVM 119 项全绿、Syscalls 47 项全绿，CPU 计费基数与 CallFlags 权限完全对齐 C#。
6. **二进制 IO 与 JSON 序列化 (49/49 passed)**：
   - VarInt、Header/Block/Tx 序列化字节流与 OrderedDictionary JSON 键序对齐 C#。
7. **最终状态**：
   - 生成总报告 `outputs/full-protocol-coverage-audit-2026-09-03.md`；
   - `cargo check --workspace` 与 `cargo fmt --all -- --check` 均以 Exit code 0 通过。

## GitHub Release v0.16.0 正式发布

- **精准暂存并提交**：按用户授权精确提交本次 13 个核心版本与代码产物（commit: `b31a7fe8`）；
- **远端推送**：推送到 `origin protocol-v3.10.1-compliance` 分支并打上 `v0.16.0` 标签；
- **GitHub Release**：成功创建并发布正式 Release：https://github.com/r3e-network/neo-rs/releases/tag/v0.16.0。
- **主分支 `main` 同步**：通过安全快进合并（commit: `17ca683a`）将 `protocol-v3.10.1-compliance` (v0.16.0) 完整合并入远端 `origin/main`，使主分支即为最新 v0.16.0。


