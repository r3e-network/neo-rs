
## VM 跨实现差分继续（2026-09-11）

- 新增 scripts/gen-chain-vm-vectors.py：逐字节解析 mainnet_blocks.json 的 27 个真实主网区块，提取 708 个唯一 invocation/verification 脚本；解析统计 27 header witnesses + 534 tx witnesses，1 个空脚本跳过。
- Rust bare VM 已执行 chain-vectors：708 个脚本，561 HALT / 147 FAULT，FAULT 均为未注册 syscall，0 harness error。
- C# runner 仍受机器级 NuGet ProgramData 缺失阻塞；尝试显式 NuGet config 和进程环境变量仍不能绕过 ConfigurationDefaults 静态初始化，需管理员修复 HKLM 后重启 WorkBuddy。

- 修正 gen-chain-vm-vectors.py 的交易属性解析：不再把属性当作通用 varbytes，而是按 Neo N3 类型逐项读取（HighPriority/OracleResponse/NotValidBefore/Conflicts/NotaryAssisted）；修正后 27 区块仍稳定产出 708 向量。新增 scripts/vm-diff.py 严格比较器已通过同输入和篡改差异测试。

- ?? FIX-MACHINE-ENV.ps1:??????????? UAC,???? PowerShell ???????????(NoExit);???????????????
