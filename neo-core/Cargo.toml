# Copyright @ 2023 - 2024, R3E Network
# All Rights Reserved

[package]
name = "neo-core"
version = "0.1.0"
edition = "2021"


#[lib]
#name       = "neo"
#crate-type = ["staticlib"]


[dependencies]
neo-base   = { path = "../neo-base", default-features = false }
neo-crypto = { path = "../neo-crypto", default-features = false }
neo-vm = { path = "../neo-vm", default-features = false }
neo-proc-macros = {path = "../neo-proc-macros", default-features = false}
neo-json = {path ="../neo-json", default-features = false}

bytes     = { version = "1", default-features = false }
subtle    = { version = "2", default-features = false }

hex              = { version = "0.4", default-features = false, features = ["alloc"] }
serde            = { version = "1", default-features = false, features = ["derive", "alloc"] }
serde_json       = { version = "1", default-features = false, features = ["alloc"]}
serde_repr       = { version = "0" }

hashbrown        = { version = "0", features = ["serde"]}
mimalloc         = { version = "0", default-features = false }

lz4_flex         = { version = "0.11", default-features = false }

thiserror        = { version = "1", optional = true }
thiserror-no-std = { version = "2", optional = true }
num-bigint = "0.4.6"

NeoRust = { git = "https://github.com/R3E-Network/NeoRust", branch = "master" }

primitive-types = "0.13.0"
bitflags = "2.6.0"
lazy_static = "1.5.0"
once_cell = "1.19.0"
murmur3 = "0.5.2"
num-traits = "0.2.19"
chrono = "0.4.38"
byteorder = "1.5.0"
encoding_rs = "0.8.34"
tokio = { version = "1.0", features = ["full"] }
actix = "0.13"
futures = "0.3"
log = "0.4"
bloomfilter = "1.0.14"
sha2 = "0.10.8"
aes-gcm = "0.11.0-pre.1"
getset = "0.1.2"
rand = "0.8.5"

sha3 = "0.10.8"
async-trait = "0.1.81"  # or the latest version

[features]
default = ["neo-base/default", "neo-crypto/default", "thiserror-no-std"]

std     = ["neo-base/std", "neo-crypto/std", "serde/std", "serde_json/std", "thiserror"]
