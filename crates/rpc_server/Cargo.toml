[package]
name = "neo-rpc-server"
version = "0.1.0"
edition = "2021"

[dependencies]
neo-core = { path = "../core" }
neo-ledger = { path = "../ledger" }
neo-persistence = { path = "../persistence" }
neo-config = { path = "../config" }
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"