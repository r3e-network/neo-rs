# Copyright @ 2023 - 2024, R3E Network
# All Rights Reserved

[package]
name = "neo-vm"
version = "0.1.0"
edition = "2021"


[dependencies]
neo-core     = { path = "../neo-core" }

bytes        = { version = "1", default-features = false }

strum        = { version = "0", default-features = false }
strum_macros = { version = "0" }


[features]
default = []

std     = []