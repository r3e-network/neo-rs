# Suggested commands
- `cargo test` — run crate unit tests.
- `cargo fmt` — format Rust code with rustfmt.
- `cargo clippy --all-targets --all-features` — lint.
- `cargo doc --open` — build docs for primitives.