# Task completion checklist
- Run `cargo fmt` and `cargo clippy --all-targets --all-features` when touching code.
- Run `cargo test` to verify unit tests pass.
- Update docs/comments if behavior changes (public API types are re-exported from lib.rs).
- Summarize changes and note any unrun commands in final response to user.