[package]
name = "neo-primitives"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
documentation.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
readme.workspace = true
description = "Primitive types for Neo blockchain (UInt160, UInt256, BigDecimal, etc.)"

[dependencies]
# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
bitflags = { workspace = true }

# Encoding
hex = { workspace = true }
bs58 = { workspace = true }

# Hashing (for address computation)
sha2 = { workspace = true }
ripemd = { workspace = true }

# Numeric types
num-bigint = { workspace = true }
num-integer = { workspace = true }
num-traits = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }

# Testable time source
chrono = { workspace = true }
parking_lot = { workspace = true }

# Mature arbitrary-precision decimal arithmetic (used by the
# `big_decimal_bigdecimal` wrapper introduced in the
# 2026-06-13-comprehensive-refactoring change, Phase C3).
bigdecimal = { workspace = true }

# NOTE: neo-primitives is a Layer 0 crate with NO neo-* dependencies
# Serializable implementations for UInt160/UInt256 are in neo-io::serializable::primitives

[dev-dependencies]
proptest = { workspace = true }

[features]
default = []
