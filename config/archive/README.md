Archived node configs
=====================

This directory stores historical sync and debugging configs that are useful for
reproducing past investigations but are not default node entry points.

The root-level `neo_mainnet_node.toml`, `neo_testnet_node.toml`, and
`neo_production_node.toml` files remain in place because the node binary,
Docker setup, scripts, and docs reference them directly.
