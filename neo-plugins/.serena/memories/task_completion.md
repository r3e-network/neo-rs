# Task completion checklist
- Run `cargo check` (and tests if relevant) to ensure build passes.
- Format code with `cargo fmt` if modified code style may change.
- Consider `cargo clippy` for lint regressions when time allows.
- Summarize changes and mention verification commands executed.