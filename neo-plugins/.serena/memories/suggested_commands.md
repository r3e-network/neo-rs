# Common commands
- Build/check: `cargo check`
- Tests: `cargo test`
- Format: `cargo fmt`
- Lint: `cargo clippy`
- Run specific plugin binaries/examples: use crate-specific binaries if added; otherwise dependents load via `PluginCollection`.