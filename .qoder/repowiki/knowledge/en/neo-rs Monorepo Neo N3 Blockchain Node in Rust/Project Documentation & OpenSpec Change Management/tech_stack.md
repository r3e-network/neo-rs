Markdown-based documentation plus the openspec tooling (configured via `openspec/config.yaml` with `schema: spec-driven`) for structured spec and change management; golden-state comparison outputs are plain JSON files generated by a protocol-consistency runner.